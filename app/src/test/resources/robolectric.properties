# Robolectric SDK config — pin to API 33 (Android 13). Picking the SDK
# explicitly avoids the per-test "downloading SDK" warning on first run
# and keeps results deterministic across CI agents that may have
# different prefetched SDKs.
sdk=33
